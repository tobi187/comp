#include <bits/stdc++.h>

using namespace std;

int main() {
    #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    #endif

    int n, t, f, s;
    cin >> n;

    for (int i = 0; i < n; i++) {
        cin >> t;
        for (int tt = 0; tt < t; tt++) {
            cin >> f >> s;
            string ss;
            cin >> ss;
 
        }
    }
}
